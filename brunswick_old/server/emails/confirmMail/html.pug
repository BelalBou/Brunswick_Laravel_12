if lang == 'en'
  p Hello #{customerName},
  p We confirm the validation of your order for #{when}, it will be treated as soon as possible.
  h4 Summary
  ul
    each menu in order.menus
      if menu.quantity == 1
        li= menu.size_en + ' ' + menu.title_en
      else
        li= menu.quantity + 'x ' + menu.size_en + ' ' + menu.title_en
      if menu.extras
        each extra in menu.extras
          span= '+ ' + extra.title_en
          br
      if menu.remark
        span= menu.remark
        br
  p Please check #[a(href='https://lunch.brunswick-marine.com') lunch.brunswick-marine.com] for more details about the orders.
else
  p Bonjour #{customerName},
  p Nous vous confirmons la validation de votre commande pour le #{when}, elle sera traitée dans les plus bref délais.
  h4 Résumé
  ul
    each menu in order.menus
      if menu.quantity == 1 
        li= menu.size + ' ' + menu.title
      else
        li= menu.quantity + 'x ' + menu.size + ' ' + menu.title
      if menu.extras
        each extra in menu.extras
          span= '+ ' + extra.title
          br
      if menu.remark
        span= menu.remark
        br
  p Merci de vérifier #[a(href='https://lunch.brunswick-marine.com') lunch.brunswick-marine.com] pour plus d'informations sur les commandes.
hr(style='border: 0.5px solid #eeeeee')
br
a(href='https://lunch.brunswick-marine.com'): img(src='https://s3.eu-west-3.amazonaws.com/imb-brunswick/Cafeteria.png' alt='Brunswick logo' title="Brunswick logo" style="display: block" width="360" height="120")